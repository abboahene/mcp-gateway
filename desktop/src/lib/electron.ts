export const fs = (window as any).require('fs');
export const path = (window as any).require('path');
export const os = (window as any).require('os');
export const child_process = (window as any).require('child_process');
export const electron = (window as any).require('electron');
